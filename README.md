# Whale 13F 버전3
Render 설정:
- Root Directory: 빈칸
- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn server:app`

GitHub 최상위에 index.html / server.py / requirements.txt 3개를 올립니다.
